export const REDIS_CONSTANTS = Object.freeze({
    REDIS: {
        MODE: {
            EX: "EX",
            REDIS_DURATION: 86400, // seconds = 24 hours
        },
    },
    REDIS_CONNECTION_FAILED: "Redis connection failed",
});
